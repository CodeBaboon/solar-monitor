# Solar Monitor — Python dependencies
# Install with: pip install -r requirements.txt
#
# Versions pinned for reproducibility on Raspberry Pi OS.
# To upgrade, replace a version with the latest and re-test.

# Modbus TCP communication with the Midnite Classic 200
pymodbus==3.6.6

# Async web framework + WebSocket support
fastapi==0.111.0

# ASGI server (runs FastAPI)
uvicorn[standard]==0.29.0

# Required by FastAPI for form/static file handling
python-multipart==0.0.9

# Required by uvicorn[standard] for fast event loop on Linux
uvloop==0.19.0

# Required by uvicorn[standard] for HTTP/1.1 keep-alive
httptools==0.6.1
